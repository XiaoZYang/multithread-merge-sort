#ifndef MERGE_H
#define MERGE_H
#include <vector>

std::vector<int> merge(std::vector<int>&, int, int);
std::vector<int> merge_sort(std::vector<int>&);
std::vector<int> merge_sorted(std::vector<int> &, std::vector<int> &);

#endif
